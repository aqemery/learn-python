import { HeroPattern } from '@/components/HeroPattern'
import { Heading } from '@/components/Heading'

export const description =
  'Learn everything there is to know about the Protocol API and integrate Protocol into your product.'

export const sections = [
  { title: 'Variable Scope', id: 'variable-scope' },
]

<HeroPattern />

# Functions

A function is a logical unit of code that executes when you call it. You can provide inputs to a function. It’s a great way to handle code that needs to be repeated

<Heading level={2} id="variable-scope" />
## Variable Scope {{ anchor: false }}



- Functions should operate on data rather than custom objects
- Functions
- Parameters
- Arguments
- Scope
- Local scope
- Default arguments
- Return
- Global variable
- “global” keyword 
