import { HeroPattern } from '@/components/HeroPattern'
import { Heading } from '@/components/Heading'

export const description =
  'Learn everything there is to know about the Protocol API and integrate Protocol into your product.'

export const sections = [
  { title: 'Raise an Exception', id: 'raise_exception' },
]

<HeroPattern />

# Exceptions

Sometimes things go wrong in your program
and you want to do something besides exit the program,
Or you want to create your own exception
that are not enforced by python

<Heading level={2} id="raise_exception" />
## Raise an Exception {{ anchor: false }}

- Exceptions
- None
- Try except else finally
- Errors should never pass silently
- Error types
- Raise exception
